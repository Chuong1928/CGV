<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <style>
         .ticket {
            width: 950px;
            background-size: 950px 375px;
         }
         .ticket .ticket__inner {
            width: 760px;
         }
         .ticket-secondary,
         .ticket-primery {
            padding-left: 30px;
            padding-right: 35px;
         }
         .ticket-position {
            display: inline-block;
            overflow: hidden;
         }
         .ticket .indecator--pre {
            margin-right: -3px;
         }
         .ticket .ticket__indecator {
            position: relative;
            border: 3px solid #dbdee1;
            width: 50px;
            float: left;
            height: 300px;
            font-family: 'PT Mono';
            font-size: 12px;
            color: #dbdee1;
         }
         .ticket .pre--text {
               transform: rotate(90deg) translateZ(0);
               margin-top: 137px;
               left: -26px;
         }
         .ticket .indecator-text {
            position: absolute;
            width: 100px;
         }
         .ticket .indecator-text:before {
            content: '';
            background-repeat: no-repeat;
            width: 80px;
            height: 16px;
            position: absolute;
            top: 0px;
            left: -95px;
         }
         .ticket .indecator-text:after {
            content: '';
            background-repeat: no-repeat;
            width: 80px;
            height: 16px;
            position: absolute;
            top: 0px;
            right: -95px;
         }
         .ticket .ticket__inner {
            border: 3px solid #dbdee1;
            overflow: hidden;
            float: left;
            height: 300px;
            width: 820px;
         }
         .ticket-secondary {
            float: left;
            text-align: left;
            padding: 47px 80px 30px 60px;
         }
         .ticket .ticket__item {
            display: block;
            margin-bottom: 10px;
            font-family: 'PT Mono';
            font-size: 14px;
            text-transform: uppercase;
            text-align: left;
         }
         .ticket .ticket__date, .ticket .ticket__time {
            display: inline-block;
            margin-right: 30px;
            margin-top: 30px;
         }
         .ticket .ticket__price {
            margin-top: 40px;
         }
         .ticket-primery {
            overflow: hidden;
            border-left: 3px double #dbdee1;
            margin-top: 37px;
            padding: 27px 35px 22px 30px;
         }
         .ticket .ticket__film {
            border-bottom: 1px solid #dbdee1;
            padding-bottom: 44px;
            margin-bottom: 28px;
         }
         .ticket .ticket__item--primery {
            font-size: 20px;
         }
         .ticket .ticket__movie {
            text-transform: none;
         }
         .ticket .ticket__place {
            color: #fe505a;
         }
    </style>
  </head>
  <body>
    <h1>Hi <%= @user.name %></h1>
    <div class="ticket">
            <div class="ticket-position">
                <div class="ticket__inner">

                    <div class="ticket-secondary">
                        <span class="ticket__item">Mã hóa đơn : <strong class="ticket__number"><%= @this_order.id %> </strong></span>
                        <span class="ticket__item ticket__date"><%= @this_order.seat_orders.last.screening.start_film_at.strftime("%d/%m/%Y") %></span>
                        <span class="ticket__item ticket__time"><%= @this_order.seat_orders.last.screening.start_film_at.strftime("%H:%M") %></span>
                        <span class="ticket__item">Phòng chiếu: <span class="ticket__cinema"><%= @this_order.seat_orders.last.screening.room.name %></span></span>
                        <span class="ticket__item">CGV: <span class="ticket__hall">Hà Nội</span></span>
                        <span class="ticket__item ticket__price">Giá : <strong class="ticket__cost"><%= @this_order.seat_orders.last.seat.seat_type.price %>(vnđ)/Vé</strong></span>
                    </div>

                    <div class="ticket-primery">
                        <span class="ticket__item ticket__item--primery ticket__film">Film<br><strong class="ticket__movie"><%= @this_order.seat_orders.last.screening.film.name %></strong></span>
                        <span class="ticket__item ticket__item--primery">SỐ ghế:
                            <span class="ticket__place">
                                <% @list_seat_of_order.each do |seat| %>
                                    <%= seat %>
                                <% end %>
                            </span>
                        </span>
                    </div>


                </div>
            </div>
        </div>
  </body>
</html>